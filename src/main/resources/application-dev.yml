# Shared-cluster DEV profile — run with:  --spring.profiles.active=dev
#
# Same Atlas cluster as prod, a SEPARATE database (default: dev), and throwaway
# data: demo seeding on, a weak dev login allowed. Nothing here is guarded by
# ProdSanityCheck (that only runs for the `prod` profile).
#
# Connection string etc. come from ONE of:
#   * env vars (MONGODB_URI, JWT_SECRET, ...)  — used on Render / any host, and
#   * ./config/dev.properties                  — a gitignored local file you fill in
#                                                (copy it from config/dev.properties.example).
# The local file, when present, wins over the env-var defaults below. Run the app
# from the project root so the relative path resolves.
spring:
  config:
    import: "optional:file:./config/dev.properties"
  data:
    mongodb:
      uri: ${MONGODB_URI}
      database: ${MONGO_DB:dev}
  autoconfigure:
    exclude:
      - de.flapdoodle.embed.mongo.spring.autoconfigure.EmbeddedMongoAutoConfiguration

app:
  demo-data:
    enabled: ${DEMO_DATA_ENABLED:true}
  mongo:
    transactions:
      enabled: true # Atlas is a replica set
  # inherits app.local-bootstrap.enabled: true from application.yml — the shared dev
  # database already has the legacy account, which LegacyDataMigration promotes.
